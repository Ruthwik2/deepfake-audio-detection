# Core deep-learning framework.
# This pulls the default PyTorch build. For a specific CPU- or CUDA-only build,
# install torch separately from https://pytorch.org/get-started/locally/ , e.g.:
#   CPU:        pip install torch --index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.1:  pip install torch --index-url https://download.pytorch.org/whl/cu121
# then `pip install -r requirements.txt` for everything else.
torch>=2.0

# Audio I/O and feature extraction.
librosa>=0.10
soundfile>=0.12
audioread>=3.0

# Numerics / ML utilities.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0

# Plotting and progress.
matplotlib>=3.7
tqdm>=4.65

# Config.
PyYAML>=6.0

# Web app.
streamlit>=1.30
